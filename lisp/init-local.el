;; only needed below company proxy
;; proxy https://stackoverflow.com/a/18697151/4499942
;; (setq url-proxy-services
;;    '(("http" . "10.50.62.14:8080")))

;; (require 'server)
;;  (and (>= emacs-major-version 23)
;;      (defun server-ensure-safe-dir (dir) "Noop" t))

(provide 'init-local)
